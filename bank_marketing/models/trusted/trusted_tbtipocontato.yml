version: 2

models:
      - name: trusted_tbtipocontato
        description: "Descricao do tipo de Contato que foi feito com o cliente"
        columns:
          - name: id_tipocontato
            description: "Chave Pk da tabela"

          - name: tipo_contato
            description: "Tipo de Contato que foi feito com o cliente"

